# TCS Authentication (Option 1: Cookies - RECOMMENDED)
# Export cookies from your browser after logging into benzin.tcs.ch
# Format: JSON string with cookie names and values
# Example: {"session": "abc123xyz", "auth_token": "def456uvw"}
TCS_COOKIES={"cookie_name": "cookie_value"}

# TCS Authentication (Option 2: Username/Password - Fallback)
# Only needed if not using cookies
TCS_USERNAME=your_email@example.com
TCS_PASSWORD=your_password

# OpenRouter API Key (for browser-use AI automation)
# Get your API key from https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Browser Automation Configuration
HEADLESS=true
# Model choice for browser-use (tested and working with OpenRouter):
# - google/gemini-2.0-flash-exp:free (RECOMMENDED - free, fast, confirmed working)
# - google/gemini-2.0-pro-exp-02-05:free (alternative, also free)
# - openai/gpt-4o (reliable but costs ~$2.50/M tokens)
# - anthropic/claude-3.5-sonnet (has validation issues with OpenRouter)
LLM_MODEL=google/gemini-2.0-flash-exp:free

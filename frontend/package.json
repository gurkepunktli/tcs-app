{
  "name": "tcs-benzinpreis-frontend",
  "version": "1.0.0",
  "description": "TCS Benzinpreis OCR Frontend",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.3.0"
  }
}
